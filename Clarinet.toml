[project]
name = 'bookrent'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.lending-manager]
path = 'contracts/lending-manager.clar'
clarity_version = 3
epoch = 'latest'

[contracts.textbook-nft]
path = 'contracts/textbook-nft.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
